# Expo version

This project pins `expo@^54.0.35` (see `package.json`) — check that field before
relying on any Expo API. Use the versioned docs at https://docs.expo.dev/versions/v54.0.0/
before writing any code; do not assume a different SDK version from memory or from an
external doc link.
